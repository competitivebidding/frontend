import React from 'react';
import styles from './FullUser.module.scss';

function FullUser({avatar, ign, id, startDate, earnedThisMonth, spentThisMonth, earned, spent}) {
    return (
        <div className={styles.user}>

        </div>
    );
}

export default FullUser;
